= Fitnesse Tools

QueryResultBuilder to help building the actual result of Fitnesse SLIM Fixtures of type Query Table.

ClasspathBuilder to help build the classpath from the output of the command mvn dependency:build-classpath 
